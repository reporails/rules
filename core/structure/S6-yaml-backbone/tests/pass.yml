# Complete backbone with all sections
version: "1.0"

maps:
  components:
    api: src/api/
    web: src/web/
  platform:
    ci: .github/workflows/

contracts:
  api-schema: contracts/api.schema.json

quick_ref:
  build: npm run build
  test: npm test
