# MyApp

Project with inline procedures.

## Deployment

When deploying, first run the test suite, then build the Docker image,
then push to the registry, then update the Kubernetes deployment,
then verify the health checks pass.

Step 1. Run tests
Step 2. Build Docker image
Step 3. Push to registry

## Commands

- `npm test` — run tests
