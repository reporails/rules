rules:
  - id: S1-root-too-long
    message: "Root instruction file exceeds 200 lines"
    severity: ERROR
    languages: [generic]
    pattern-regex: "(?s)(?:[^\\n]*\\n){200,}"
    paths:
      include:
        - "{{main_instruction_file}}"
