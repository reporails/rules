# CI workflow that validates CLAUDE instruction files
name: instruction-check
on: [push]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: reporails validate
