rules:
  - id: G5-no-contracts
    message: "Backbone lacks contracts key"
    severity: WARNING
    languages: [yaml]
    patterns:
      - pattern: |
          $KEY: $VALUE
      - pattern-not: |
          contracts: $VALUE
    paths:
      include:
        - ".reporails/backbone.yml"
