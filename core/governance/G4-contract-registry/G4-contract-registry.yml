rules:
  - id: G4-no-contracts
    message: "Backbone lacks contracts key"
    severity: WARNING
    languages: [yaml]
    patterns:
      - pattern-regex: "(?s)\\A[\\s\\S]+"
      - pattern-not-regex: "(?m)^contracts:"
    paths:
      include:
        - ".reporails/backbone.yml"
