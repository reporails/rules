# MyApp

Data processing pipeline.

## Testing

Use pytest with test_*.py naming convention.
Run `pytest -x --tb=short` for fast feedback during development.

## Commands

- `pytest tests/` — run all tests
