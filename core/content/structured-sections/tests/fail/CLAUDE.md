# MyApp

This is a backend service for user management. It is built with Express and TypeScript. To run the tests you should use npm test and for building the production version use npm run build. The source code lives in the src directory and tests are in the tests directory. You should never commit env files and always run the linter before pushing code. The database uses PostgreSQL and migrations should be run with npm run migrate.
