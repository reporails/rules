rules:
  - id: "CORE.C.0015.check.0001"
    message: "Structured sections (## headings) found in instruction file"
    severity: WARNING
    languages: [generic]
    pattern-regex: "^## "
    paths:
      include:
        - "{{instruction_files}}"
