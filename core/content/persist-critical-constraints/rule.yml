rules:
  - id: "CORE.C.0016.check.0001"
    message: "Strong directive found in instruction file"
    severity: WARNING
    languages: [generic]
    pattern-regex: "(?i)(NEVER|ALWAYS|MUST|CRITICAL|IMPORTANT)"
    paths:
      include:
        - "{{instruction_files}}"
