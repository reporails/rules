# MyApp

SaaS platform.

## Workflow

Feature branches must follow `feature/JIRA-NNN` naming.
PRs require 2 approvals before merge to main.
Deploy to staging automatically on merge to develop.

## Commands

- `npm test` — run tests
