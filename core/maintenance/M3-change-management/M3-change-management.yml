rules:
  - id: M3-no-change-process
    message: "Change management indicators"
    severity: WARNING
    languages: [generic]
    pattern-regex: "(?i)(approval|change.*process|breaking.*change|deprecat)"
    paths:
      include:
        - "{{instruction_files}}"
        - "CONTRIBUTING.md"
