default_agent: claude
# Project-level configuration for rules/ repo
exclude_dirs:
  - tests

disabled_rules:
  - "CORE:G:0001"     # Cross-Agent Compatibility — excluded in agent config
  - "CORE:C:0027"     # MCP Configuration Documented — project does not use MCP
  - "CORE:G:0006"     # Managed/Enterprise Config — no enterprise deployment
  - "CORE:G:0008"     # MCP Config File — project does not use MCP
  - "CORE:S:0022"     # Local Override File Present — not required for this project
  - "CLAUDE:S:0005"   # Hook Event Type Names — no settings.json in repo
  - "CLAUDE:S:0006"   # Hook Handler Type Field — no settings.json in repo
  - "CLAUDE:S:0007"   # Hook Handler Prompt Field — no settings.json in repo
  - "CLAUDE:G:0001"   # Hook Shell Commands Project Dir — no settings.json in repo
  - "CORE:S:0020"     # Hook Event Handlers Defined — no settings.json in repo
  - "CORE:S:0021"     # Settings Scope Declared — no settings.json in repo
  - "CORE:G:0005"     # Permission Config Denies Sensitive Access — no settings.json in repo
  - "CLAUDE:S:0004"   # Hook Handler Command Field — no hooks configured yet
