# MyApp

Project that depends on user-level settings.

Developers must configure their ~/.claude/settings.json with the project API key
before starting work. Without the user-level setting, tests will fail.

## Commands

- `npm test` — run tests
- `npm run build` — production build
