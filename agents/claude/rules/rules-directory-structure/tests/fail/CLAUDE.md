# MyApp

Project with rules directory containing non-markdown files.

## Commands

- `npm test` — run tests
- `npm run build` — production build

## Structure

```
src/        # Application code
tests/      # Test suite
.claude/    # Agent configuration
```
